import { Divider, Typography } from "@mui/material";
import Stack from "@mui/material/Stack";
import React, { useEffect } from "react";

const Home = React.memo(() => {
  return <Stack direction="column">Home</Stack>;
});

export default Home;
